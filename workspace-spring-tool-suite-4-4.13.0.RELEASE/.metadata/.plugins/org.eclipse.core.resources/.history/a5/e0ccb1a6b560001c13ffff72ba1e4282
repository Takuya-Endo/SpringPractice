<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
	<head>
		<meta charset="UTF-8">
		<title>SpringSample</title>
	</head>
	<body>
		<h1>SpringSample</h1>
		<a th:href="@{/practice}">一覧へ</a>
		
		<table border="1">
			<tr>
				<th>ID</th>
				<td th:text="${user.id}"></td>
			</tr>
			<tr>
				<th>NAME</th>
				<td th:text="${user.name}"></td>
			</tr>
			<tr>
				<th>AGE</th>
				<td th:text="${user.age}"></td>
			</tr>
		</table>
		
		<div>
			<form th:action="@{/practice/delete}" method="post">
				<input type="hidden" th:field="${userDeleteForm.id}" th:value="${user.id}" />
				<input type="hidden" th:field="${userDeleteForm.name}" th:value="${user.name}" />
				<input type="hidden" th:field="${userDeleteForm.age}" th:value="${user.age}" />
				<input type="submit" value="削除" />
			</form>
		</div>
		
	</body>
</html>